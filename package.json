{
  "name": "a-line-remembered",
  "version": "1.0.0",
  "description": "A minimalist procedural Fourier puzzle game",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "ios:sync": "npm run build && cap sync ios",
    "ios:open": "npm run build && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "pixi.js": "^8.18.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  }
}
